package commands;

/**
 * An enum which contains possible command types for {@link CommandDescription} objects.
 */

public enum CommandType {
    ARGUMENTLESS,
    PERSON_SINGLE_ARGUMENT,
    EXIT,
    EXECUTE_SCRIPT;
}
